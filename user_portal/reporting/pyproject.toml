[project]
name = "iceberg-workspaces-reporting"
version = "0.1.0"
description = "Isolated live Iceberg reporting with dbt Charts"
requires-python = ">=3.13,<3.14"
dependencies = [
    "dbt-charts==0.8.0",
    "dbt-duckdb==1.10.0",
    "duckdb==1.5.5",
    "httpx>=0.28.1",
    "sqlglot==30.18.0",
]
